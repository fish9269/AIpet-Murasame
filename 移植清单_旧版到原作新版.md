# 移植清单：旧版 → 原作新版（双轨制迁移作业指导）

> 生成日期：2026-09-07
> 旧版（本仓库）：commit `bf23dae`，基于原作旧版绿色客户端 + 本地长期自定义
> 原作新版：`D:\下载\AI桌宠\1\AIpet-Murasame-upstream`，commit `685eaa7`（v1.16.1）
> 原则：**旧版只存档、不 merge**；新功能一律在新版源码目录上逐个移植，跑通一项提交一项。

---

## 一、两版差异总览（已机器比对）

| 状态 | 数量 | 说明 |
|---|---|---|
| 两版完全相同 | 213 个文件 | 基础资产/多数模块未动 → 新版与旧版同源，移植难度可控 |
| 两版均有但内容不同 | 81 个文件 | 重点处理区（下表二/三） |
| 仅旧版有（你的独占） | 37 个文件 | 整组搬移/参考（下表四） |
| 仅新版有 | 少量 | 新版新文件，全部保留 |

## 二、你独占的功能（上游没有，需搬过去）

| 功能 | 关键文件 | 搬运方式 |
|---|---|---|
| Galgame 好感度玩法 | `qq/qq_galgame.py`、`qq/qq_commands.py`、`qq/qq_chat.py`、`qq/qq_bridge.py`（免@口令） | 新文件直接拷贝；`qq_chat/commands/bridge` 里注入点需按**新版接口**重接（旧文件不可整体覆盖，见第三节） |
| 成人（限制级）模式 | `qq/qq_adult.py`、`qq/qq_chat.py`、`qq/qq_commands.py` | 同上 |
| 网络用语自动查询 | `qq/qq_slang.py`、`qq/qq_chat.py` | 同上 |
| 空闲自动离线 | `qq/qq_bridge.py`（后台线程）、`qq/qq_config.py`、`pcl_launcher/widgets.py` | 旧逻辑片段合入新版对应文件（上游无 `qq_auto_offline_*` 键） |
| 活泼模式（群聊接话） | `qq/qq_bridge.py`、`qq/qq_config.py` | 同上（上游无 `qq_lively_*` 键） |
| 群记忆按人分仓 | `qq/qq_memory.py` 等 | 上游 `qq_memory.py` 与你版不同，逐函数对照合入 |
| 主人白名单扩展 | `qq/qq_config.py`（`qq_master_ids` 键上游没有） | 合入上游配置解析 |
| 插件体系（含官方/我的分类） | `plugins/`（7 个插件目录）、`pcl_launcher/plugins_panel.py` | 目录整体拷贝；面板页面挂到新版启动器页面栈 |
| 主题体系（官方/我的、重命名、背景等） | `pcl_launcher/themes/`、`pcl_launcher/themes_panel.py`、`pcl_launcher/colors.py` 的主题部分 | 新版 `colors.py` 与旧版差异很大，需**以新版为基础重新套主题层** |
| 启动器背景/预览区壁纸 | `pcl_launcher/main_window.py`（GL 背景） | 新版 main_window 也有自身演进，逐段对照移植 |
| 时间同步守护 | `time_sync_guard.py`、相关 bat、`plugins/time_guard/` | 整组拷贝 |
| 更新日志 | `更新日志/*.md` | 直接拷贝，继续按日记录 |

## 三、两版都改过、不能整文件覆盖的核心文件（逐文件人工合入）

| 文件 | 旧版是你的大改 | 新版是作者的演进 | 处理 |
|---|---|---|---|
| `qq/qq_bridge.py` | 50 KB（活泼/离线/调度） | 38 KB（自成一版） | 以新版为骨架，把你功能片段插回；跑通一次测一轮 |
| `qq/qq_chat.py` | 28 KB | 12 KB | 同上（成人/galgame/slang 注入点） |
| `qq/qq_commands.py` | 14 KB | 7 KB | 新版指令为准 + 追加你的指令 |
| `qq/qq_config.py` | 含白名单/新键 | 精简 | 新版解析上补你的键 |
| `qq/qq_memory.py` | 按人分仓 | 有自己改动 | 合入分仓逻辑 |
| `pcl_launcher/widgets.py` | 97 KB（主题/插件 UI） | 88 KB（作者自增） | 新版为基础，叠加主题化与插件入口 |
| `pcl_launcher/main_window.py` | 72 KB（背景/GL/主题重启） | 50 KB | 同上 |
| `pcl_launcher/colors.py` | 11 KB（主题系统） | 2.6 KB | 新版之上加主题加载层 |
| `tool/chat.py`、`classes/murasame_class.py`、`run.py`、`main.py` | 小幅旧改 | 比你新（更大/更新） | **以新版为准**，你如无必要改动就直接用新版 |

> 注：`qq/qq_offline.py` 两版字节一致 —— 离线补拉无需处理。

## 四、推荐移植顺序（每次 1 项，测通再提交）

1. **先跑通新版本体**：新版目录内双击 `install.bat`（或手动 venv + requirements）→ `启动桌宠.bat` 能起立绘 → 证明环境 OK。
2. 移植 **QQ 侧**（性价比高、逻辑独立）：`qq_galgame.py`/`qq_adult.py`/`qq_slang.py` 整文件拷入 → 在 `qq_chat.py`+`qq_commands.py`+`qq_bridge.py` 按新版接口接好 → 启动 `启动QQ.bat` 逐项实测（/help、开启galgame模式、查看好感度、成人模式、网络用语）。
3. 移植 **离线/活泼/记忆分仓/白名单**：这些嵌在 bridge/config/memory 里，跟着第三节逐文件合。
4. 移植 **插件体系 + 主题体系 + 更新日志按钮**：新版启动器结构不同，建议先在旧版 repo 里把"新版对应文件 vs 旧版功能"的 diff 打出来，再手工搬到新版（有需要可让我协助生成逐文件 diff）。
5. 全部完成后：在新版目录建自己的 git（init + 首次提交），或直接作为旧仓库的 `new-base` 分支重新 root（推荐后者保留全部历史脉络）。

## 五、常用操作速查

```bat
REM 查看两版某文件差异（旧版这边执行，upstream 已 fetch/克隆在旁）
"C:\Program Files\Git\bin\git.exe" -C D:\下载\AI桌宠\1\AIpet-Murasame diff --no-index ^
  "D:\下载\AI桌宠\1\AIpet-Murasame\qq\qq_chat.py" ^
  "D:\下载\AI桌宠\1\AIpet-Murasame-upstream\qq\qq_chat.py"
```
```bat
REM 新版环境（已布置中：D:\下载\AI桌宠\1\AIpet-Murasame-upstream\runtime\venv）
cd /d D:\下载\AI桌宠\1\AIpet-Murasame-upstream
runtime\venv\Scripts\python.exe run.py        REM 桌宠本体
runtime\venv\Scripts\python.exe run_launcher.py  REM 源码模式启动器（改 UI 即时生效，不需打包）
```

## 六、隐私红线（搬运时同样遵守）

- 永远不提交：`config.json`（API Key）、`data/`、`face_shibie/`、`NapCat.Shell.Windows.OneKey/config`、`pets/*/memory/`（真实聊天记录）。
- 新版目录如建 git，先照抄旧仓库的 `.gitignore`（旧仓库根目录已备好一份可直接复制）。
