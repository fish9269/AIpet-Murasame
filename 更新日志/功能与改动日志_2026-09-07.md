# AIpet-Murasame — 功能与改动日志

> 记录日期：2026-09-07
> 适用范围：启动器（AIpet-Murasame.exe / pcl_launcher/）UI、主题系统、插件管理与聊天模块联动
> 说明：QQ 侧玩法（Galgame/成人模式/记忆分仓/活泼模式等）见同目录《功能与改动日志_2026-09-06.md》

---

## 一、插件管理页（完整版）

- **定位说明更正**：插件页统一管控**程序全部功能插件**（桌宠侧 + QQ 侧），不再局限于 QQ；人脸识别、时间同步守护等桌宠功能与活泼/Galgame 等 QQ 功能同页管理
- 每张插件卡片提供：启用开关、`⚙ 设置`（按 plugin.json 的 settings 声明自动生成表单并写回 config.json）、`📁 打开位置`、`🗑 删除`
- **分类筛选**：全部插件 / 官方插件 / 我的插件（带数量角标）
  - 官方插件 = 随程序自带（`plugin.json` 写入 `builtin: true`，现有 7 个已全部标记：空闲自动离线、活泼模式、成人模式、Galgame、网络用语、人脸识别、时间同步守护）
  - 我的插件 = 通过「导入插件 (zip)」导入的第三方插件（导入时自动写入 `builtin: false`，卡片带「我的」橙色标记）
- **切换流畅度优化**：切分类只做卡片显隐，不再整表重建（原先每次销毁重建 7 张卡片导致明显卡顿），并加 160ms 淡入过渡动画
- 文件：`pcl_launcher/plugins_panel.py`、`plugins/<id>/plugin.json`

## 二、主题系统

- **主题目录页分区**：🏛 官方主题 / 📁 我的主题 两个分区并显示数量
  - 官方主题（classic 旧版主题、senrenbanka 千恋万花·樱华）：不可删除；卡片含 应用 / 导出 / 重命名 / 打开目录 / **复制为我的**
  - 我的主题（导入或复制产生）：可 应用 / **修改背景（图片或视频）** / 导出 / 重命名 / 打开目录 / 删除
- **全部主题卡片**都新增「✏️ 重命名」（弹窗改名，写入 theme.json 的 name）与「📂 打开目录」（资源管理器直达主题文件夹，方便替换 assets 图片）
- 「📑 复制为我的」：把官方主题复制一份（id 加 `_copy` 后缀、`builtin=false`）后即可自由改背景等细节，官方主题保持原样
- 导入的主题 zip 一律强制标记为「我的主题」，即使原包导自官方主题
- 主题数据结构：`themes/<id>/theme.json`（id / name / builtin / accent / colors / decor 樱花装饰 / nav_icons 导航按钮图 / background 背景 / widgets 圆角等）
- 千恋万花主题已应用：自定义按钮图标（图标文件夹素材）、背景图片/视频壁纸、樱花装饰、卡通圆角按钮
- 文件：`pcl_launcher/themes_panel.py`、`pcl_launcher/colors.py`、`pcl_launcher/themes/`

## 三、启动器外观与背景

- 移除标题栏左上角绿色方块 logo、左侧「模型列表」标题旁的绿色方块图标（顶部只留导航按钮）
- 左侧模型列表改回**白色底**（不再透明露底）
- 右侧 Live2D 预览区背景：主题壁纸**整张拉伸填满**预览区（GLSL 330 着色器 + legacy 双路径），保证看到完整整图；无壁纸时用主题底色，不再出现白块/透明露桌面/只显示半张图
- 右侧各卡片/输入框/弹窗采用白色半透明底，主题壁纸可透出
- 窗口背景支持：主题壁纸图片（cover 铺满）、视频循环播放、透明度调节
- 文件：`pcl_launcher/main_window.py`、`pcl_launcher/widgets.py`

## 四、人脸识别联动

- 人脸管理功能并入「插件 → 人脸识别」（照片库在顶部「人脸」页，识别参数在插件设置里）
- 停用「人脸识别」插件 → 顶部「人脸」导航自动隐藏；重新启用自动恢复
- 文件：`pcl_launcher/main_window.py`、`pcl_launcher/widgets.py`

## 五、设置页更新日志入口（本次新增）

- 设置界面最底部新增 **📜 更新日志** 区块，三个按钮：
  - `查看日志`：弹窗阅读本文件夹内最新一篇更新日志
  - `导出日志`：把最新日志另存为副本（.md）
  - `打开目录`：在资源管理器打开 `更新日志` 文件夹
- 本文件即通过该文件夹维护：`更新日志/功能与改动日志_YYYY-MM-DD.md`
- 文件：`pcl_launcher/widgets.py`

## 六、打包与回退

- 打包仍走 PyInstaller onedir（ASCII 路径虚拟环境 `C:\aipet_build_venv`），产物为根目录 `AIpet-Murasame.exe` + `_internal/`
- 每次发布前旧版本备份于 `tmp/backup_old_exe_20260906/`（后缀字母递增），需要回退时恢复对应 `AIpet-Murasame.exe.bak_*` 与 `_internal.bak_*` 即可

---

**已知设计行为**：官方主题不可删除；改名对官方与我的主题均开放；插件「删除」对官方插件仍保留二次确认（内置插件删除后失去管控入口，请谨慎）。

## 七、热修：视频主题背景导致启动器闪退（追加）

- **现象**：在主题「背景」里选择视频后启动器必崩（Windows 事件日志：`Qt5Multimedia.dll` 异常 `0xC00000FD` 栈溢出）。
- **根因（两个叠加）**：
  1. 无边框+透明窗口上放 `QVideoWidget` 原生视频层（DirectShow 引擎）→ Qt 内部递归崩溃；
  2. 更深一层：即使不用 QVideoWidget，`QMediaPlaylist` **循环播放**在视频播到结尾自动 Loop 重启时也会在 `Qt5Multimedia.dll` 内部递归栈溢出（启动约 18~20 秒、即第一轮播完时必现）。
- **修复**（`pcl_launcher/main_window.py`）：
  - 弃用 QVideoWidget → 自定义 `_VideoBackgroundSurface`（QAbstractVideoSurface）把视频帧**软件解码成 QImage**，由定时器逐帧 cover 裁切刷新到背景 QLabel（透明/圆角/半透明全部兼容）；
  - 弃用播放列表 → `setMedia` 单曲 + `EndOfMedia` 信号在 Python 侧手动 stop/setPosition(0)/play 实现循环。
  - 实测：视频背景连续运行 65 秒以上跨多个循环稳定，无新崩溃记录。
- **顺带**：千恋万花主题顶部导航按键图标 26px → 34px。

## 八、视频背景改为 OpenCV 解码 + Live2D 区同步（追加）

- **空白根因**：Qt5.15 的 WMF/DirectShow 多媒体引擎在本机（Win11 26200）对 H.264 mp4 **打开即 InvalidMedia / 栈溢出**（无论 QVideoWidget 还是自定义 QAbstractVideoSurface 均不可用）→ 视频一直无帧、区域空白。
- **修复**：视频背景改用 **OpenCV(ffmpeg) 解码线程**（`_VideoBgReader`，帧信号→QLabel 逐帧刷新，播完自动回卷），完全绕开 QtMultimedia。实测用户 74MB/1080p30 H.264 视频持续渲染（600+ 帧），无崩溃。
- **Live2D 预览区同步**：预览区 GL 每帧取最新视频帧镜像上传为纹理，视频主题下不再显示白底；图片主题仍走原壁纸纹理路径。
- 设置页按功能分区：①基础信息与密钥 ②对话模型与推理 ③长文本输出 ④语音合成与识别 ⑤Live2D 与立绘 ⑥QQ 聊天配置 ⑦微信 ClawBot 配置 ⑧桌宠显示与空闲行为。

## 九、预览区背景按原始比例显示（追加）

- Live2D 预览区背景由「拉伸填满」改为 **fit 等比完整显示**：图片/视频按自身宽高比完整呈现、居中，四周留边不再变形（图片与视频统一，着色器与 legacy 双路径同步修改）。

## 十、预览区背景改 cover 中心裁切填满 + 侧栏去白底（追加）

- 预览区背景由 fit 留边改为 **cover 中心裁切填满**：等比放大铺满整个区域、居中裁切边缘，无空白不变形（着色器与 legacy 双路径同步）。
- 左侧模型列表所在侧栏**移除白色大块底**，改为透明露出主题背景/视频（标题「模型列表」区域不再被白底遮挡）。

## 十一、「模型列表」标题加白色描边（追加）

- 侧栏透明化后标题在视频/图片背景上不易辨认 → 「模型列表」改为自定义绘制文字：白色圆角描边 + 主题色填充（QLabel QSS 不支持描边，用 QPainterPath strokePath 实现）。

## 十二、导航与模型名统一白色描边（追加）

- 顶部图片导航按钮（模型/设置/记忆/桌宠/提示词/插件/主题等）与左侧模型卡片名称全部加白色描边（主题色填充）。
- 模型名称描边在 `add_model` 生成处实现 → 以后**新增的模型自动带描边**，不只限现有两个。
- 实现说明：整体自绘按钮曾触发 Qt5Core 断言崩溃（0xC0000409）→ 改为**原生 QPushButton（高亮/图标不变）+ 描边文字子 QLabel 叠加**（鼠标穿透），模型名/「模型列表」沿用已验证安全的 QPainterPath 描边类。

## 十三、导航描边文字消失修复（追加）

- 原因：文字移入子 QLabel 后按钮本体文本为空，布局只给图标宽度，描边文字层被挤成 0 宽不可见。
- 修复：`_NavOutlineButton.fit_to_content()` 按「图标+间距+文字」设置按钮最小宽度；子标签跟随图标右侧放置并留边。a11y 树验证 7 个导航文字节点全部恢复。

## 十四、导航按钮文字自适应与稳定性定稿（追加）

- 文字裁切原因：文字随“原生居中图标”摆放时可用宽度不足。
- 定稿方案（多轮崩溃排查结论）：**绝不在 QPushButton 上自绘**（两版自绘均触发 Qt5Core 0xC0000409 断言崩溃）；改为按钮本体保留原生 QSS 高亮胶囊 + **子 Widget（_NavContentLabel）自绘图标与白色描边文字**（仅用 drawPixmap/QPainterPath 安全原语），整组居中、`fit_to_content()` 按 图标+间距+文字 设置最小宽度 → 文字完整显示且按钮随文字长度自适应。实测 75 秒+ 无崩溃。

## 十五、导航按钮宽高双自适应（追加）

- 高度自适应：按钮最小高度 = max(图标高, 文字行高)+8。
- 宽度自适应补漏：超宽图标素材限宽 56px 等比缩小（防止整行被撑爆、右侧文字被裁）。

## 十六、设置页顶部分类标签（追加）

- 设置页顶部新增：**全部配置 / 桌宠配置 / QQ配置 / 微信配置** 切换标签。
- 点击即切换到对应配置界面：全部配置 = 原来的完整列表；桌宠配置 = 基础密钥/对话推理/长文本/语音视觉/Live2D立绘/桌宠显示/通用页脚；QQ配置 = QQ 聊天配置分区；微信配置 = 微信 ClawBot 分区。
- 实现：各功能分区包进可显隐的框（_open_box/_cat_entries），标签切换只改显隐不重建；a11y 验证 QQ 页仅剩 QQ 分区。

## 十七、主题色移入主题目录 + 设置新增「其他」分类（追加）

- 设置页顶部标签新增 **其他配置**；「更新日志（查看/导出/打开目录）」从设置页脚移入其他配置分类。
- 「主题色（强调色）」选择块从设置页移除，改放在**主题目录页**顶部（与主题一体管理），切换行为与原先一致（即时渐变预览）；设置页全部/桌宠分类不再包含主题色。

## 十八、启动器窗口全局圆角（追加）

- 主窗口改为**圆角窗口**：在 showEvent/resizeEvent 里按窗口尺寸生成圆角遮罩（QRegion 圆角路径，半径 18px），四角由透明背景呈现圆角。
- **全局生效与主题无关**：不读任何主题配置，任何主题/无壁纸状态都是圆角；缩放窗口时自动重新生成遮罩。

## 十九、圆角修复：改用遮罩方案（追加）

- 现象：WA_TranslucentBackground（分层窗口）会**忽略 setMask**，导致上一版圆角遮罩不生效、仍是直角。
- 修复：去掉窗口级透明属性，改为「不透明窗口 + QRegion 圆角遮罩」（Windows 上经典圆角做法）；窗口底色用主题 Color8 填充，主题壁纸/视频标签与各控件全部正常叠加，四角由遮罩裁成圆角、露出桌面。全局生效、与主题无关。

## 二十、圆角定稿：透明窗口 + 父层圆角底色绘制（追加）

- 上一版改不透明+遮罩导致主题背景视频消失、配色异常（分层叠层被破坏）。
- 回退透明窗口（背景/视频/半透明控件恢复正常叠层），全局圆角改由 **PCLMainWindow.paintEvent 绘制圆角底色**（QPainterPath 圆角填充主题 Color8，抗锯齿）：四角之外透明露桌面，内部主题底/壁纸/视频自然叠上；与主题无关、随窗口尺寸自适应（resize 触发重绘）。日志确认视频帧与 Live2D GL 同步同时正常。

## 二十一、圆角：背景标签与标题栏同步圆角裁切（追加）

- 上一版父层圆角底色被正方形背景标签/标题栏盖回方形角 → 圆角“失效”。
- 修复：新增 `_RoundedBgLabel`（图片/视频壁纸标签按窗口圆角换算半径裁切绘制）；标题栏 paintEvent 同样按圆角路径裁切；配合父层圆角底色，四角完整呈现圆角。主题无关、全局生效；背景视频/主题色叠层保持正常（日志确认 300+ 帧持续渲染）。

## 二十二、窗口圆角最终实现（追加，待实机确认）

- 需求澄清：窗口整体圆角，顶部目录条若为方形会盖住上方圆角。
- 实现（bb）：透明窗口保留（壁纸/视频叠层正常）；壁纸/视频标签与顶部目录条均按「窗口四角 26px 圆角」换算裁切（标签半径 18px = 26-8 内边距），四角同步裁圆；底部角落同样由壁纸标签裁切。
- 说明：本会话的截图像素验证与实机合成可能不一致，需在真实屏幕确认四角效果。

## 二十三、圆角去描边 + 背景满铺裁切（追加）

- 去掉窗口四周 8px 内边距（此前透明边距在实机显示成一圈“描边”）。
- 壁纸/视频背景满铺整个窗口并按窗口圆角 r=26 裁切四角；顶部目录条满铺到窗口边缘、只圆上两角（r=26）与窗口上角一致，下沿平直衔接内容区；下方两角由背景裁切。

## 二十四、提示词/模型页左右贴边对齐（追加）

- 模型预览页与提示词编辑器去掉左右外边距，内容左右贴到窗口边缘，与顶部目录条同宽（上下保留少量间距），消除“内容比目录窄一截”的穿帮。

## 二十八、QQ 对话调节 + 活泼模式重复回复修复

- 设置 → QQ配置 → 新增「📐 对话调节」：
  - 单次回复最多字数（qq_max_reply_chars，0=不限）：回复超过上限自动截断；
  - 每次对话最多回复次数（qq_max_replies_per_conversation，0=不限）：同一会话 10 分钟内连续对话计数，达上限后保持沉默，10 分钟无回复自动重置。
- 活泼模式重复回复同一问题修复（三层）：
  1. 收到被 @ 消息的瞬间即占用该群活泼冷却位（last_bot_talk=now），杜绝活泼模式在正常回复前把同一话题再插嘴；
  2. 正常回复成功后照旧清空群话题缓冲并记冷却；
  3. 活消息去重：同一 message_id 120 秒内重复上报直接忽略（NapCat 偶发双报/与补拉撞车不再二次回答）。

## 二十九、NapCat 自动登录（QQ号+密码，免扫码）

- 设置 → QQ配置 → 新增「🔓 NapCat 自动登录（免扫码）」：
  - 自动登录开关（qq_auto_login_enable，默认关）
  - 登录 QQ 号（qq_login_uin）
  - QQ 密码（qq_login_password，输入框打码；仅存本地 config.json）
- 启动 NapCat 前自动注入环境变量 NAPCAT_QUICK_ACCOUNT 与 NAPCAT_QUICK_PASSWORD_MD5（密码 MD5 传参）；start_napcat.bat 手动启动同样读取 config.json 注入。
- 说明：密码直登由 NapCat 支持；若触发设备/新设备/异常验证（140022008/10/11），NapCat 控制台会给出验证步骤，首次或换设备仍可能需要一次人工处理。

## 三十、自动登录修复：环境变量传递 + 即改即存

- 控制台提示“未配置回退密码环境变量”→ 原 bat 用独立 PowerShell 进程设置的环境变量无法传回 cmd；改为 PS 把变量写入临时文件、`for /f` 导入当前控制台后再启动 NapCat。
- 对话调节/自动登录 5 个控件（字数上限、回复次数、自动登录开关、QQ号、密码）**改动即自动写盘**，不再依赖「保存」按钮。

## 三十一、NapCat 打不开修复 + 自动登录链路验证

- 排查：此前给 start_napcat.bat 内嵌注入导致批处理语法错误（NapCat 无法启动）。
- 修复：start_napcat.bat 恢复为稳定原版（含 QQData 独立数据目录）；自动登录改用**独立包装脚本 start_napcat_autologin.bat**（for/f 导入 napcat_login_env.py 输出的环境变量后 call 原版）。
- 实测控制台链路：环境变量注入成功（账号+MD5）→ 快速登录（无历史记录）→ 密码回退登录执行 → 返回“帐户名或密码不正确”→ 说明机制已通，失败为凭据问题（需核对 QQ 号/密码，或该号需先设/验证 QQ 密码）。
