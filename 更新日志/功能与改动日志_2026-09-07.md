# AIpet-Murasame — 功能与改动日志

> 记录日期：2026-09-07
> 适用范围：启动器（AIpet-Murasame.exe / pcl_launcher/）UI、主题系统、插件管理与聊天模块联动
> 说明：QQ 侧玩法（Galgame/成人模式/记忆分仓/活泼模式等）见同目录《功能与改动日志_2026-09-06.md》

---

## 一、插件管理页（完整版）

- **定位说明更正**：插件页统一管控**程序全部功能插件**（桌宠侧 + QQ 侧），不再局限于 QQ；人脸识别、时间同步守护等桌宠功能与活泼/Galgame 等 QQ 功能同页管理
- 每张插件卡片提供：启用开关、`⚙ 设置`（按 plugin.json 的 settings 声明自动生成表单并写回 config.json）、`📁 打开位置`、`🗑 删除`
- **分类筛选**：全部插件 / 官方插件 / 我的插件（带数量角标）
  - 官方插件 = 随程序自带（`plugin.json` 写入 `builtin: true`，现有 7 个已全部标记：空闲自动离线、活泼模式、成人模式、Galgame、网络用语、人脸识别、时间同步守护）
  - 我的插件 = 通过「导入插件 (zip)」导入的第三方插件（导入时自动写入 `builtin: false`，卡片带「我的」橙色标记）
- **切换流畅度优化**：切分类只做卡片显隐，不再整表重建（原先每次销毁重建 7 张卡片导致明显卡顿），并加 160ms 淡入过渡动画
- 文件：`pcl_launcher/plugins_panel.py`、`plugins/<id>/plugin.json`

## 二、主题系统

- **主题目录页分区**：🏛 官方主题 / 📁 我的主题 两个分区并显示数量
  - 官方主题（classic 旧版主题、senrenbanka 千恋万花·樱华）：不可删除；卡片含 应用 / 导出 / 重命名 / 打开目录 / **复制为我的**
  - 我的主题（导入或复制产生）：可 应用 / **修改背景（图片或视频）** / 导出 / 重命名 / 打开目录 / 删除
- **全部主题卡片**都新增「✏️ 重命名」（弹窗改名，写入 theme.json 的 name）与「📂 打开目录」（资源管理器直达主题文件夹，方便替换 assets 图片）
- 「📑 复制为我的」：把官方主题复制一份（id 加 `_copy` 后缀、`builtin=false`）后即可自由改背景等细节，官方主题保持原样
- 导入的主题 zip 一律强制标记为「我的主题」，即使原包导自官方主题
- 主题数据结构：`themes/<id>/theme.json`（id / name / builtin / accent / colors / decor 樱花装饰 / nav_icons 导航按钮图 / background 背景 / widgets 圆角等）
- 千恋万花主题已应用：自定义按钮图标（图标文件夹素材）、背景图片/视频壁纸、樱花装饰、卡通圆角按钮
- 文件：`pcl_launcher/themes_panel.py`、`pcl_launcher/colors.py`、`pcl_launcher/themes/`

## 三、启动器外观与背景

- 移除标题栏左上角绿色方块 logo、左侧「模型列表」标题旁的绿色方块图标（顶部只留导航按钮）
- 左侧模型列表改回**白色底**（不再透明露底）
- 右侧 Live2D 预览区背景：主题壁纸**整张拉伸填满**预览区（GLSL 330 着色器 + legacy 双路径），保证看到完整整图；无壁纸时用主题底色，不再出现白块/透明露桌面/只显示半张图
- 右侧各卡片/输入框/弹窗采用白色半透明底，主题壁纸可透出
- 窗口背景支持：主题壁纸图片（cover 铺满）、视频循环播放、透明度调节
- 文件：`pcl_launcher/main_window.py`、`pcl_launcher/widgets.py`

## 四、人脸识别联动

- 人脸管理功能并入「插件 → 人脸识别」（照片库在顶部「人脸」页，识别参数在插件设置里）
- 停用「人脸识别」插件 → 顶部「人脸」导航自动隐藏；重新启用自动恢复
- 文件：`pcl_launcher/main_window.py`、`pcl_launcher/widgets.py`

## 五、设置页更新日志入口（本次新增）

- 设置界面最底部新增 **📜 更新日志** 区块，三个按钮：
  - `查看日志`：弹窗阅读本文件夹内最新一篇更新日志
  - `导出日志`：把最新日志另存为副本（.md）
  - `打开目录`：在资源管理器打开 `更新日志` 文件夹
- 本文件即通过该文件夹维护：`更新日志/功能与改动日志_YYYY-MM-DD.md`
- 文件：`pcl_launcher/widgets.py`

## 六、打包与回退

- 打包仍走 PyInstaller onedir（ASCII 路径虚拟环境 `C:\aipet_build_venv`），产物为根目录 `AIpet-Murasame.exe` + `_internal/`
- 每次发布前旧版本备份于 `tmp/backup_old_exe_20260906/`（后缀字母递增），需要回退时恢复对应 `AIpet-Murasame.exe.bak_*` 与 `_internal.bak_*` 即可

---

**已知设计行为**：官方主题不可删除；改名对官方与我的主题均开放；插件「删除」对官方插件仍保留二次确认（内置插件删除后失去管控入口，请谨慎）。

## 七、热修：视频主题背景导致启动器闪退（追加）

- **现象**：在主题「背景」里选择视频后启动器必崩（Windows 事件日志：`Qt5Multimedia.dll` 异常 `0xC00000FD` 栈溢出）。
- **根因（两个叠加）**：
  1. 无边框+透明窗口上放 `QVideoWidget` 原生视频层（DirectShow 引擎）→ Qt 内部递归崩溃；
  2. 更深一层：即使不用 QVideoWidget，`QMediaPlaylist` **循环播放**在视频播到结尾自动 Loop 重启时也会在 `Qt5Multimedia.dll` 内部递归栈溢出（启动约 18~20 秒、即第一轮播完时必现）。
- **修复**（`pcl_launcher/main_window.py`）：
  - 弃用 QVideoWidget → 自定义 `_VideoBackgroundSurface`（QAbstractVideoSurface）把视频帧**软件解码成 QImage**，由定时器逐帧 cover 裁切刷新到背景 QLabel（透明/圆角/半透明全部兼容）；
  - 弃用播放列表 → `setMedia` 单曲 + `EndOfMedia` 信号在 Python 侧手动 stop/setPosition(0)/play 实现循环。
  - 实测：视频背景连续运行 65 秒以上跨多个循环稳定，无新崩溃记录。
- **顺带**：千恋万花主题顶部导航按键图标 26px → 34px。

## 八、视频背景改为 OpenCV 解码 + Live2D 区同步（追加）

- **空白根因**：Qt5.15 的 WMF/DirectShow 多媒体引擎在本机（Win11 26200）对 H.264 mp4 **打开即 InvalidMedia / 栈溢出**（无论 QVideoWidget 还是自定义 QAbstractVideoSurface 均不可用）→ 视频一直无帧、区域空白。
- **修复**：视频背景改用 **OpenCV(ffmpeg) 解码线程**（`_VideoBgReader`，帧信号→QLabel 逐帧刷新，播完自动回卷），完全绕开 QtMultimedia。实测用户 74MB/1080p30 H.264 视频持续渲染（600+ 帧），无崩溃。
- **Live2D 预览区同步**：预览区 GL 每帧取最新视频帧镜像上传为纹理，视频主题下不再显示白底；图片主题仍走原壁纸纹理路径。
- 设置页按功能分区：①基础信息与密钥 ②对话模型与推理 ③长文本输出 ④语音合成与识别 ⑤Live2D 与立绘 ⑥QQ 聊天配置 ⑦微信 ClawBot 配置 ⑧桌宠显示与空闲行为。

## 九、预览区背景按原始比例显示（追加）

- Live2D 预览区背景由「拉伸填满」改为 **fit 等比完整显示**：图片/视频按自身宽高比完整呈现、居中，四周留边不再变形（图片与视频统一，着色器与 legacy 双路径同步修改）。

## 十、预览区背景改 cover 中心裁切填满 + 侧栏去白底（追加）

- 预览区背景由 fit 留边改为 **cover 中心裁切填满**：等比放大铺满整个区域、居中裁切边缘，无空白不变形（着色器与 legacy 双路径同步）。
- 左侧模型列表所在侧栏**移除白色大块底**，改为透明露出主题背景/视频（标题「模型列表」区域不再被白底遮挡）。

## 十一、「模型列表」标题加白色描边（追加）

- 侧栏透明化后标题在视频/图片背景上不易辨认 → 「模型列表」改为自定义绘制文字：白色圆角描边 + 主题色填充（QLabel QSS 不支持描边，用 QPainterPath strokePath 实现）。

## 十二、导航与模型名统一白色描边（追加）

- 顶部图片导航按钮（模型/设置/记忆/桌宠/提示词/插件/主题等）与左侧模型卡片名称全部加白色描边（主题色填充）。
- 模型名称描边在 `add_model` 生成处实现 → 以后**新增的模型自动带描边**，不只限现有两个。
- 实现说明：整体自绘按钮曾触发 Qt5Core 断言崩溃（0xC0000409）→ 改为**原生 QPushButton（高亮/图标不变）+ 描边文字子 QLabel 叠加**（鼠标穿透），模型名/「模型列表」沿用已验证安全的 QPainterPath 描边类。

## 十三、导航描边文字消失修复（追加）

- 原因：文字移入子 QLabel 后按钮本体文本为空，布局只给图标宽度，描边文字层被挤成 0 宽不可见。
- 修复：`_NavOutlineButton.fit_to_content()` 按「图标+间距+文字」设置按钮最小宽度；子标签跟随图标右侧放置并留边。a11y 树验证 7 个导航文字节点全部恢复。

## 十四、导航按钮文字自适应与稳定性定稿（追加）

- 文字裁切原因：文字随“原生居中图标”摆放时可用宽度不足。
- 定稿方案（多轮崩溃排查结论）：**绝不在 QPushButton 上自绘**（两版自绘均触发 Qt5Core 0xC0000409 断言崩溃）；改为按钮本体保留原生 QSS 高亮胶囊 + **子 Widget（_NavContentLabel）自绘图标与白色描边文字**（仅用 drawPixmap/QPainterPath 安全原语），整组居中、`fit_to_content()` 按 图标+间距+文字 设置最小宽度 → 文字完整显示且按钮随文字长度自适应。实测 75 秒+ 无崩溃。

## 十五、导航按钮宽高双自适应（追加）

- 高度自适应：按钮最小高度 = max(图标高, 文字行高)+8。
- 宽度自适应补漏：超宽图标素材限宽 56px 等比缩小（防止整行被撑爆、右侧文字被裁）。

## 十六、设置页顶部分类标签（追加）

- 设置页顶部新增：**全部配置 / 桌宠配置 / QQ配置 / 微信配置** 切换标签。
- 点击即切换到对应配置界面：全部配置 = 原来的完整列表；桌宠配置 = 基础密钥/对话推理/长文本/语音视觉/Live2D立绘/桌宠显示/通用页脚；QQ配置 = QQ 聊天配置分区；微信配置 = 微信 ClawBot 分区。
- 实现：各功能分区包进可显隐的框（_open_box/_cat_entries），标签切换只改显隐不重建；a11y 验证 QQ 页仅剩 QQ 分区。

## 十七、主题色移入主题目录 + 设置新增「其他」分类（追加）

- 设置页顶部标签新增 **其他配置**；「更新日志（查看/导出/打开目录）」从设置页脚移入其他配置分类。
- 「主题色（强调色）」选择块从设置页移除，改放在**主题目录页**顶部（与主题一体管理），切换行为与原先一致（即时渐变预览）；设置页全部/桌宠分类不再包含主题色。

## 十八、启动器窗口全局圆角（追加）

- 主窗口改为**圆角窗口**：在 showEvent/resizeEvent 里按窗口尺寸生成圆角遮罩（QRegion 圆角路径，半径 18px），四角由透明背景呈现圆角。
- **全局生效与主题无关**：不读任何主题配置，任何主题/无壁纸状态都是圆角；缩放窗口时自动重新生成遮罩。

## 十九、圆角修复：改用遮罩方案（追加）

- 现象：WA_TranslucentBackground（分层窗口）会**忽略 setMask**，导致上一版圆角遮罩不生效、仍是直角。
- 修复：去掉窗口级透明属性，改为「不透明窗口 + QRegion 圆角遮罩」（Windows 上经典圆角做法）；窗口底色用主题 Color8 填充，主题壁纸/视频标签与各控件全部正常叠加，四角由遮罩裁成圆角、露出桌面。全局生效、与主题无关。

## 二十、圆角定稿：透明窗口 + 父层圆角底色绘制（追加）

- 上一版改不透明+遮罩导致主题背景视频消失、配色异常（分层叠层被破坏）。
- 回退透明窗口（背景/视频/半透明控件恢复正常叠层），全局圆角改由 **PCLMainWindow.paintEvent 绘制圆角底色**（QPainterPath 圆角填充主题 Color8，抗锯齿）：四角之外透明露桌面，内部主题底/壁纸/视频自然叠上；与主题无关、随窗口尺寸自适应（resize 触发重绘）。日志确认视频帧与 Live2D GL 同步同时正常。

## 二十一、圆角：背景标签与标题栏同步圆角裁切（追加）

- 上一版父层圆角底色被正方形背景标签/标题栏盖回方形角 → 圆角“失效”。
- 修复：新增 `_RoundedBgLabel`（图片/视频壁纸标签按窗口圆角换算半径裁切绘制）；标题栏 paintEvent 同样按圆角路径裁切；配合父层圆角底色，四角完整呈现圆角。主题无关、全局生效；背景视频/主题色叠层保持正常（日志确认 300+ 帧持续渲染）。

## 二十二、窗口圆角最终实现（追加，待实机确认）

- 需求澄清：窗口整体圆角，顶部目录条若为方形会盖住上方圆角。
- 实现（bb）：透明窗口保留（壁纸/视频叠层正常）；壁纸/视频标签与顶部目录条均按「窗口四角 26px 圆角」换算裁切（标签半径 18px = 26-8 内边距），四角同步裁圆；底部角落同样由壁纸标签裁切。
- 说明：本会话的截图像素验证与实机合成可能不一致，需在真实屏幕确认四角效果。

## 二十三、圆角去描边 + 背景满铺裁切（追加）

- 去掉窗口四周 8px 内边距（此前透明边距在实机显示成一圈“描边”）。
- 壁纸/视频背景满铺整个窗口并按窗口圆角 r=26 裁切四角；顶部目录条满铺到窗口边缘、只圆上两角（r=26）与窗口上角一致，下沿平直衔接内容区；下方两角由背景裁切。

## 二十四、提示词/模型页左右贴边对齐（追加）

- 模型预览页与提示词编辑器去掉左右外边距，内容左右贴到窗口边缘，与顶部目录条同宽（上下保留少量间距），消除“内容比目录窄一截”的穿帮。

## 二十八、QQ 对话调节 + 活泼模式重复回复修复

- 设置 → QQ配置 → 新增「📐 对话调节」：
  - 单次回复最多字数（qq_max_reply_chars，0=不限）：回复超过上限自动截断；
  - 每次对话最多回复次数（qq_max_replies_per_conversation，0=不限）：同一会话 10 分钟内连续对话计数，达上限后保持沉默，10 分钟无回复自动重置。
- 活泼模式重复回复同一问题修复（三层）：
  1. 收到被 @ 消息的瞬间即占用该群活泼冷却位（last_bot_talk=now），杜绝活泼模式在正常回复前把同一话题再插嘴；
  2. 正常回复成功后照旧清空群话题缓冲并记冷却；
  3. 活消息去重：同一 message_id 120 秒内重复上报直接忽略（NapCat 偶发双报/与补拉撞车不再二次回答）。

## 二十九、NapCat 自动登录（QQ号+密码，免扫码）

- 设置 → QQ配置 → 新增「🔓 NapCat 自动登录（免扫码）」：
  - 自动登录开关（qq_auto_login_enable，默认关）
  - 登录 QQ 号（qq_login_uin）
  - QQ 密码（qq_login_password，输入框打码；仅存本地 config.json）
- 启动 NapCat 前自动注入环境变量 NAPCAT_QUICK_ACCOUNT 与 NAPCAT_QUICK_PASSWORD_MD5（密码 MD5 传参）；start_napcat.bat 手动启动同样读取 config.json 注入。
- 说明：密码直登由 NapCat 支持；若触发设备/新设备/异常验证（140022008/10/11），NapCat 控制台会给出验证步骤，首次或换设备仍可能需要一次人工处理。

## 三十、自动登录修复：环境变量传递 + 即改即存

- 控制台提示“未配置回退密码环境变量”→ 原 bat 用独立 PowerShell 进程设置的环境变量无法传回 cmd；改为 PS 把变量写入临时文件、`for /f` 导入当前控制台后再启动 NapCat。
- 对话调节/自动登录 5 个控件（字数上限、回复次数、自动登录开关、QQ号、密码）**改动即自动写盘**，不再依赖「保存」按钮。

## 三十一、NapCat 打不开修复 + 自动登录链路验证

- 排查：此前给 start_napcat.bat 内嵌注入导致批处理语法错误（NapCat 无法启动）。
- 修复：start_napcat.bat 恢复为稳定原版（含 QQData 独立数据目录）；自动登录改用**独立包装脚本 start_napcat_autologin.bat**（for/f 导入 napcat_login_env.py 输出的环境变量后 call 原版）。
- 实测控制台链路：环境变量注入成功（账号+MD5）→ 快速登录（无历史记录）→ 密码回退登录执行 → 返回“帐户名或密码不正确”→ 说明机制已通，失败为凭据问题（需核对 QQ 号/密码，或该号需先设/验证 QQ 密码）。

## 三十二、移除 NapCat 密码自动登录功能

- 该号手机可登但 NapCat 密码直登被腾讯拒绝（报“密码不正确”，机制与 MD5 均验证无误，判定为风控/账号策略限制），按用户要求**整体删除**：
  - 设置页移除「NapCat 自动登录（免扫码）」区块及三个配置项；
  - 移除启动器环境变量注入、napcat_login_env.py、start_napcat_autologin.bat；
  - config.json/config.example.json 清理相关键（本地已保存的密码一并清除）。
- start_napcat.bat 保持原版；登录请走 NapCat 原版扫码（登录一次后本地会话可自动快速登录，无需每次扫码）。


## 三十三、QQ AIpet 关闭时联动关闭 NapCat（本次新增）

- **问题现象**：在启动器点击「⏹ 关闭 QQ AIpet」（或直接关闭启动器窗口）只终止了 run_qq.py 进程，由启动器自动拉起的 NapCat 控制台窗口（start_napcat.bat → NapCatWinBootMain.exe → bootmain\QQ.exe 进程树）原样残留，QQ 机器人看着"还开着"，但消息已无人处理。
- **修复方案**（`pcl_launcher/main_window.py`）：
  1. 启动器自动拉起 start_napcat.bat 时保存 Popen 句柄并记录 `_napcat_started_by_us = True`；
  2. 关闭 QQ AIpet / 关闭启动器时调用新增 `_kill_napcat_if_ours()`：若 NapCat 由本启动器本次会话拉起，用 `taskkill /F /T /PID` 按**进程树**连带关闭（cmd 控制台 → NapCatWinBootMain → QQ 全家一起退出）；
  3. Popen 句柄失效（如 NapCat 窗口曾被手动关闭但主进程残留）时走兜底：按“本绿色版目录 + NapCatWinBootMain.exe / bootmain\QQ.exe”特征用 PowerShell 清理，**不会误伤**别处安装的正式版 QQ（D:\QQ）；
  4. **不误杀规则**：NapCat 为手动启动（双击 bat）、或等待登录期间用户主动取消（选择保留窗口接管）时 `_napcat_started_by_us` 置 False，关闭 QQ AIpet 只关 run_qq、保留 NapCat。
- 相关函数：`_kill_qq_process`、新增 `_kill_napcat_if_ours`、`_ensure_napcat_async`、`_on_qq_clicked`（取消等待分支）。
- 打包：启动器壳重新 PyInstaller 打包部署（旧版备份 `tmp/backup_old_exe_20260906/AIpet-Murasame.exe.bak_20260907a`）。重启启动器后生效。

## 三十四、AI 实时时间与天气查询能力（本次新增）

- **问题现象**：AI 回答"现在几点 / 今天星期几"时只能靠猜或含糊其辞——短文本链路只注入"日期 + 早上/晚上"粗时段，QQ / 长文本链路连日期都没注入；问"今天天气"则完全没有数据来源，模型只能编造。
- **修复内容**：
  1. **精确时间**（`tool/time_utils.py`）：`build_time_context()` 由"日期+时段"升级为精确到分钟的完整时间，形如 `2026年09月07日（星期一）21:19（晚上）`——模型每轮都能如实报出当前几点几分；
  2. **实时天气工具**（新增 `tool/weather_utils.py`，全部免费、无需 API Key）：
     - 城市定位：优先读 config.json 顶层 `weather_city`（支持中文/拼音城市名，或"纬度,经度"坐标）；留空时用 ip-api.com 按出口 IP 自动定位；
     - 城市名→坐标：Open-Meteo Geocoding API（支持中文）；
     - 天气数据：Open-Meteo Forecast API（温度/体感/湿度/风/天气代码），WMO 代码自映射中文（晴/多云/阴/雨/雪/雷等），风速换算蒲福风级、风向转 16 方位中文；
     - 输出一行中文摘要，如：`【实时天气】南京（江苏）现在 24°C，晴；体感 27°C，湿度 78%；东北风1级，数据更新于 21:15。`
     - 可靠性：每次 HTTP 6 秒超时、结果进程内缓存 30 分钟、失败一律静默返回 None——天气服务故障绝不影响对话主流程；
  3. **四条对话链路全部注入**（时间每轮必带；天气仅在**命中提问关键词**如"天气/几度/下雨/冷不冷/带伞"时才联网查询）：
     - QQ / 微信：`qq/qq_chat.py chat_once`——用户消息前加 `[时间]` 与（命中时）天气行，只影响发给模型的内容，**不写入会话记忆**（记忆仍存原文，避免历史污染）；
     - 桌宠长文本：`longtext/longtext_manager.py _chat_stream`——同上注入；
     - 桌宠短文本（云端 DeepSeek/Qwen）：`tool/cloud_API_chat.py cloud_talk`——时间原有注入升级为精确时间，天气按需并入；
     - 桌宠短文本（本地 Qwen3-LoRA）：`tool/chat.py qwen3_lora`——同上。
  4. **人设提示词说明**（`pets/murasame/prompt.txt` 与 `longtext_prompt.txt`）：新增"系统实时信息"说明——问几点/几号直接照方括号内时间如实回答；天气数据未附带时如实说暂时不清楚，**绝对不要编造**。
- **配置**（config.json / config.example.json 新增，均在设置页"保存"类操作下保留）：
  - `weather_city`：天气城市，默认 `""`（按 IP 自动定位；网络出口变化会导致城市漂移，固定城市可填中文名，如 `"南京"`）；
  - `weather_enable`：天气查询总开关，默认 `"true"`。
- 验证：天气工具端到端实测通过（南京自动定位 + 实时数据 + 意图识别正反例）；全部改动文件编译通过、模块导入正常。

## 三十五、设置页保存按钮固定右下角（本次新增，全局生效）

- **问题现象**：设置页（含 全部配置/桌宠配置/QQ配置/微信配置/其他配置 全部分类）内容很长，「💾 保存」按钮原来躺在滚动内容流的末尾，每次修改配置后都要**滚动到底部**才能找到按钮保存，很不方便。
- **修改方案**（`pcl_launcher/widgets.py`，类 `PCLSettingsPanel`）：
  1. 设置面板从 `QScrollArea` 改为 `QWidget` 外壳 + 内嵌滚动区（滚动区样式不变：透明无边框），保持"设置内容可滚动"的行为；
  2. **保存按钮从滚动内容中移出**，放到滚动区**外**的固定底部条（右下角、右对齐），滚动任何设置内容按钮都始终停留在右下角，不用再滑到底部去找；
  3. **全局生效**：本面板是启动器唯一的设置宿主，所有分类共用同一固定底条，非单个主题/单个分类专属；切换到 QQ配置/其他配置 等任意分类时保存按钮同样固定可见；
  4. 原有按钮样式保留（主题色渐变风格），并补充 `pressed` 按压态；固定条与滚动区垂直相邻、无重叠，内容不会穿过按钮。
- 验证：offscreen 运行验证——主保存按钮不在滚动内容容器内；滚动条拉到底后按钮仍位于面板可视区右下角。
- 打包：启动器壳重新 PyInstaller 打包部署（旧版备份 `tmp/backup_old_exe_20260906/AIpet-Murasame.exe.bak_20260907b`）。重启启动器后生效。
- **样式调整（同日）**：保存按钮进一步改为**正圆形**（60×60，主题色渐变 + 白描边 + hover/pressed 态），只保留 💾 图标，鼠标悬停有 tooltip「保存全部配置」说明；仍固定在右下角、不随滚动移动。打包备份：`tmp/backup_old_exe_20260906/AIpet-Murasame.exe.bak_20260907c`（同日后续将直径由 46 调大到 60，备份 `...bak_20260907d`）。


## 三十六、修复：QQ「对话调节」（每次回复字数 / 每次对话回复次数）不生效

- **问题现象**：把「每次对话最多回复次数」调成 2，QQ 里仍回复超过 2 次，字数上限也不生效。
- **根因（三处叠加）**：
  1. **设置页的两个调节 spin 从未写盘**：`_wire_autosave` 虽已实现但从未绑定到这两个控件，`_save_config` 的保存循环也漏了它们 → config.json 里根本没有 `qq_max_reply_chars` / `qq_max_replies_per_conversation`，运行中的 QQ 读到默认 0=不限（用户调的 2 实际没保存进去）；
  2. **运行中的 QQ 桥接用启动快照 `self.cfg`**：即使配置写盘，旧代码也只认启动时的值，运行中修改不生效；
  3. **活泼模式发言完全绕过限制**：`_handle_lively` 不检查次数/字数上限（且活泼模式开着、每 3 分钟可自动接话一次），群聊回复次数还按"人"分仓计数（每人各 2 次，而非整群 2 次）。
- **修复内容**：
  1. `pcl_launcher/widgets.py`（设置页）：
     - 两个 spin 绑定 `_wire_autosave("spin", ...)`——**改动即写盘**，无需点保存；
     - `_save_config` 保存循环与 `_load_current_config` 显示循环补上这两个键（重启启动器后 spin 显示实际值）；
  2. `qq/qq_bridge.py`（QQ 桥接）：
     - 新增 `_reply_limits()`：**实时读取 config.json**（每次判断现读），运行中改设置立即生效、无需重启 QQ；
     - 新增 `_convo_dim_key()`：计数维度统一——私聊按 QQ 号、**群聊按整个群**（剥离按人分仓 `_u` 后缀，活泼 `lively_<群号>` 归并到群），上限真正限制同一群 10 分钟内的总回复量；
     - 次数上限判定**提前到调用模型之前**（达上限直接静默，不再白白消耗一次模型请求），拒绝回复时同步标记消息已处理（防重连补拉重复回复）；
     - 活泼模式 `_handle_lively` 一并纳入限制：先判次数上限（达上限本轮活泼静默），发送成功后与同群 @ 回复共用计数，字数上限同样生效（200 字防刷屏兜底保留）。
  3. `config.json` 补回 `qq_max_replies_per_conversation: 2`（用户此前设置未落盘，现补回；`qq_max_reply_chars: 0`）。
- **生效方式**：启动器与 QQ AIpet 均需重启一次；此后在设置页调整这两个数值**即时生效**，无需重启 QQ。
- 打包备份：`tmp/backup_old_exe_20260906/AIpet-Murasame.exe.bak_20260907e`。


## 三十七、修复：QQ 私聊"一次回复被拆成多条消息"让回复次数上限看起来失效

- **问题现象**：已把「每次对话最多回复次数」设为 2，私聊时一条消息仍收到 5 条回复（日志为 第1/5句…第5/5句）。
- **根因**：QQ 私聊回复为模仿真人聊天，会**按标点切句后逐条发送**（一次 AI 回复 = 多条 QQ 消息），而回复次数上限按"模型回复轮数"计数——用户看到一轮发出 5 条消息，自然认为"回了 5 次、超了 2 次"。
- **修复**（`qq/qq_bridge.py`）：
  - 新增模块函数 `cap_clauses_count(clauses, max_msgs)`：切句后条数超过「每次对话最多回复次数」时，前 N-1 句原样逐条发送，**其余句子合并进最后一条**一起发出——内容完整不丢失，且**一次回复发出的消息条数不超过 N**；
  - `_send_private_reply` 发送前应用该合并（N 来自对话调节配置，运行中实时生效；0=不限保持原切句行为）。
  - 例：上限 2 + 5 句回复 → 第1句 + (第2~5句合并) = 2 条消息。
  - 指令回复（/clear 等）本就是整条发送，不受影响；群聊/活泼回复本就是整条发送，维持 10 分钟窗口轮数限制不变。
- 生效方式：重启 QQ AIpet（qq_bridge 源码改动，无需重打包启动器）。


## 三十八、修正：对话调节取消"N轮后静默"，只限制单条回复的消息条数

- **问题现象**：按「每次对话最多回复次数 = 2」理解，聊到第 3 轮后 bot 完全不再回话（10 分钟窗口静默），用户不想要这种"聊几句就闭嘴"。
- **语义澄清（按用户确认）**：「每次对话最多回复次数」实际要表达的是——**一条消息触发的回复最多拆成几条消息发送**（如 2 = 一次回复最多 2 条），而不是"回 2 轮后沉默"。
- **修改**（`qq/qq_bridge.py`）：
  - **删除"N 轮后静默"逻辑**：移除 `_convo_allowed` / `_convo_note` / `_convo_dim_key` 及 `self._convo` 计数状态，私聊/群聊/活泼发言不再受轮数静默限制——每条消息都会正常回复，再不会出现"不回话"；
  - 保留并明确单轮约束：字数上限（`_cap_reply`）+ 私聊拆句条数上限（发送层 `cap_clauses_count` 合并，一次回复最多 N 条消息、内容不丢）；
  - 活泼模式只受原有冷却/200 字防刷屏控制。
- **设置页文案同步**（`pcl_launcher/widgets.py`）：控件名改为「单条回复最多发几条消息（0=不限）」，与行为一致；config 键不变（`qq_max_replies_per_conversation`）。
- 打包备份：`tmp/backup_old_exe_20260906/AIpet-Murasame.exe.bak_20260907f`。生效：重启启动器 + 重启 QQ AIpet。


## 三十九、根治 NapCat 自动登录：切换到正式版 QQ 9.9.30 官方注入模式

- **问题**：NapCat 每次重启都要求扫码（"快速登录失败，未找到该 QQ 历史登录记录"）。
- **根因排查过程**（多轮实验）：① 登录记录文件 login.enc 中始终没有 bot 条目；② 无论桌面正式版 QQ 是否运行，NapCat 自带的 QQ 9.9.33 独立版(bootmain)扫码登录后**任何位置都不落盘**（QQData/Roaming\QQ/Partitions 全部零写入）；③ 日志早有警告 `PacketBackend 不支持当前QQ版本架构 9.9.33-51802`——NapCat 4.18.19 与 QQ 9.9.33 **版本不匹配**（快速登录与包协议均不受支持）。
- **修复**：改用 NapCatShell 官方注入模式（launcher-user.bat）——NapCat 4.18.19 注入**系统已安装的正式版 QQ 9.9.30**（版本匹配）：
  - 启动日志确认：`自动快速登录成功: 3975377307`，无二维码弹窗；`NativePacketClient Hook 初始化成功`（版本警告消失）；
  - 重启 NapCat 从此**自动登录、无需扫码**。
- **配套修改**（pcl_launcher/main_window.py）：
  - 启动器 QQ 按钮改启动 launcher-user.bat（注入 9.9.30）而非 9.9.33 独立版；
  - 新增检测：正式版 QQ 正在运行时点启动会提示先关闭（注入模式与桌面 QQ 共用数据目录，不能同开；主人号用手机 QQ 不受影响）。
- **使用规则**：NapCat 运行时请勿同时开桌面正式版 QQ；手动启动 NapCat 用 `NapCat.Shell.Windows.OneKey\NapCat\launcher-user.bat`。
- 打包备份：tmp/backup_old_exe_20260906/AIpet-Murasame.exe.bak_20260908a。


## 四十、群聊图片识别：回复前检测群近期图片并识图注入

- **需求**：bot 在群里回复（被 @ 或活泼接话）时，应检测群里是否有人发了图片，识别图片内容后一起带进回复上下文，让回复能"看到"图（如"发图后 @ 问这是什么"、"大家在聊一张图时活泼接话"）。
- **实现**（qq/qq_bridge.py）：
  1. 新增 `_message_has_image` / `_note_group_image`：群消息含图片时，把该消息段存入群缓存的 `last_img`（只存元数据，不下载不识别；与活泼开关无关，群里发图就记录）；
  2. 新增 `_group_recent_image_desc(group_id, max_age=240)`：调度线程内取本群最近一张图 → 走 NapCat get_image + 云端视觉模型识别 → 返回描述；4 分钟内有效，识别成功即清空缓存（描述随回复文本进入会话记忆，后续轮可引用），失败静默不阻塞回复；
  3. 群 @ 回复：本条消息带图优先识别本条；否则查群近期图片，识别描述以"（群里有人发了一张图片，内容大概是：…）"附注并入发给模型的文本；
  4. 活泼接话：接话前同样取群近期图片描述并入输入；
  5. 支持"@bot + 纯图片(无文字)"：不再直接忽略，识别图片后回复（@ 且无图无字才忽略）。
- 生效方式：重启 QQ AIpet（桥接已重启，新代码加载中）。


## 四十一、识图"无法取得图片"修复：无图消息不再空跑 + 调度线程禁用不安全 WS 取图

- **问题**：日志频繁出现"⚠ 无法取得图片（本地路径/URL/get_image 均失败），跳过识图"。
- **根因**：① 私聊分支对**每条消息**（含纯文字）都调用识图流程，无图消息必然打印该警告（噪音）；② 群回复/活泼接话在**调度线程**内识别图片，而 `get_image` 需要独占 `ws.recv()`，会与收包线程抢帧 → file_id 型图片（无本地文件、无下载 URL）在群路径必然失败。
- **修复**（qq/qq_bridge.py）：
  1. `_extract_private_image` 开头先判消息是否真的含图（`_message_has_image`），纯文字消息直接返回，不再打印"无法取得图片"噪音；
  2. 函数增加 `allow_ws` 参数：私聊路径（收包线程）保留 NapCat get_image；群 @ 回复 / 活泼接话 / 群近期图识别（调度线程）传 `allow_ws=False`——只走线程安全的**本地路径 / 消息自带 URL 下载**，file_id-only 图片打印明确提示后跳过，绝不与收包线程抢 WS；
  3. 实测 9.9.30 注入模式的图片消息都带下载 URL（multimedia.nt.qq.com.cn），URL 下载识别正常。
- 生效方式：QQ AIpet 已重启加载。


## 四十二、修复群识图"识别成别人发的图"

- **问题**：群里被 @ 回复时，有概率识别成别人发的图而非应回复的那张。
- **根因**：群回复任务进入调度队列后会排队数秒~数十秒，而"群最近图片"缓存是全局最新一张——处理任务时若群里又有人发了新图，就会识别成新图（张冠李戴）。
- **修复**（qq/qq_bridge.py）：
  1. 群 @ 消息**入队时快照**该群当时的最近图片（`img_ref`，同一 dict 引用），随任务入队；
  2. `_group_recent_image_desc` 支持传入快照：回复处理时识别**快照里的那张图**，而不是处理时刻的最新图；
  3. 清理逻辑按对象身份判断——仅当缓存仍指向同一张图才移除，排队期间到达的新图不会被误删；
  4. 活泼接话等无快照场景仍实时取最近图。
- 生效方式：QQ AIpet 已重启加载。
